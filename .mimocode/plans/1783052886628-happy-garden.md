# Plan: Separate Docs from Dashboard

## Goal
Move docs page out of `/dashboard/docs` to standalone `/docs` route, so it can be expanded for multiple product docs (API, curl-for-typescript, etc.).

## Changes

### 1. Create new route: `src/app/docs/layout.tsx`
- Standalone layout, no DashboardShell dependency
- Simple header with back-to-home link

### 2. Move page: `src/app/dashboard/docs/page.tsx` → `src/app/docs/page.tsx`
- Keep all existing content
- Update "Back" links to point to `/` instead of `/dashboard`

### 3. Update links
- `src/app/dashboard/page.tsx` — update API Docs link to `/docs`
- `src/components/navbar/FloatingNavbar.tsx` — update docs nav item to `/docs`
- `src/components/navbar/FloatingNavbar.tsx` — update active state detection

### 4. Delete old route
- Remove `src/app/dashboard/docs/` directory

## Verification
- `vercel build` passes
- `/docs` loads standalone
- Dashboard still works
- Links from dashboard point to `/docs`
